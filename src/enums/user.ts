export enum USER_ROLES {
  SUPER_ADMIN = 'SUPER_ADMIN',
  ADMIN = 'ADMIN',
  USER = 'USER',
  PROVIDER = 'PROVIDER',
}

export enum USER_STATUS {
  pending='pending', 
  active='active', 
  blocked='blocked', 
  delete='delete'
}
